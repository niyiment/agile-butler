services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=butler_secret'
      - 'POSTGRES_USER=butler'
    ports:
      - '5432'
  redis:
    image: 'redis:latest'
    ports:
      - '6379'
